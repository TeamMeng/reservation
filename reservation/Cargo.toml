[package]
name = "reservation"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
abi = { version = "0.1.0", path = "../abi" }
async-trait = "0.1.83"
chrono = { workspace = true }
sqlx = { workspace = true }
thiserror = "1.0.64"
