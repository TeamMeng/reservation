[package]
name = "reservation"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
sqlx = { version = "0.8.2", features = [
    "postgres",
    "runtime-tokio",
    "tls-rustls",
    "chrono",
    "uuid",
] }
